import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  row: {
    marginTop: 20,
    backgroundColor: '#fff',
    alignSelf: 'stretch'
  },
  parametrs: {
    fontSize: 20
  }
});