import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  row: {
    marginTop: 50,
    backgroundColor: '#fff',
    alignSelf: 'stretch'
  },
  parametrs: {
    fontSize: 20
  }
});